"""
OWDB REST API v1.

Read-only (GET) endpoints for the core catalog, registered from
owdb_django/owdbapp/api_urls.py and pulled into the site under /api/ by a
single include() in owdb_django/urls.py. See that urls.py comment before
adding new top-level routes anywhere else in this package.
"""
